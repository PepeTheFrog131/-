# TODO Написать свою реализацию функции для подсчёта числа вхождение элементов в список
 # Инициализируем счётчик нулём
    count = 0

    # Перебираем каждый элемент списка l
    for element in l:
        # Если текущий элемент совпадает с искомым item,
        # увеличиваем счётчик на 1
        if element == item:
            count += 1

    # Возвращаем результат — количество найденных вхождений
    return count
lst = [1, 2, 3, 2, 4, 2]
print(my_count(lst, 2))  # Вывод: 3
